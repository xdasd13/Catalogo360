<!DOCTYPE html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Catálogo inteligente | Inicio</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" href="/img/favicon.ico" />

    <link
      rel="stylesheet"
      href="https://unpkg.com/swiper@9/swiper-bundle.min.css"
    />
    <link rel="stylesheet" href="/assets/css/tailwind.css" />
    <link rel="stylesheet" href="/assets/css/index-home.css" />
  </head>
  <body
    class="font-sans bg-app-gray text-app-dark"
    x-data="catalogApp()"
    x-init="init()"
    data-currency-api-key="<%= currencyApiKey || '' %>"
  >
    <div class="min-h-screen flex flex-col">
      <header class="nav-header">
        <div class="max-w-6xl mx-auto px-6 py-5">
          <div class="nav-top">
            <a href="/" class="nav-logo" aria-label="Ir al inicio">
              <img src="/img/favicon.ico" alt="Catalogo360" class="nav-logo-icon" />
              <span class="nav-logo-text">Catalogo360</span>
            </a>
            <div class="nav-actions">
              <div class="currency-switcher">
                <button
                  type="button"
                  class="currency-btn"
                  @click="currencyMenuOpen = !currencyMenuOpen"
                  :aria-expanded="currencyMenuOpen"
                >
                  <iconify-icon icon="solar:money-bag-bold-duotone" width="18" height="18"></iconify-icon>
                  <span x-text="selectedCurrency"></span>
                </button>
                <div
                  class="currency-menu"
                  x-show="currencyMenuOpen"
                  x-transition
                  @click.outside="currencyMenuOpen = false"
                  x-cloak
                >
                  <template x-for="option in currencyOptions" :key="option.code">
                    <button
                      type="button"
                      @click="changeCurrency(option.code)"
                      :class="{ 'is-active': selectedCurrency === option.code }"
                    >
                      <span class="currency-symbol" x-text="option.symbol"></span>
                      <div>
                        <p class="currency-label" x-text="option.label"></p>
                        <p class="currency-hint" x-show="option.code !== 'PEN'">
                          <span x-text="exchangeRates[option.code] ? '1 PEN ≈ ' + exchangeRates[option.code].toFixed(3) + ' ' + option.code : 'Actualizando…'"></span>
                        </p>
                      </div>
                    </button>
                  </template>
                </div>
              </div>
              <a href="/carrito" class="nav-cart" aria-label="Ver carrito">
                <iconify-icon icon="streamline:shopping-cart-basket-1-shopping-basket" width="20" height="20"></iconify-icon>
              </a>
              <% if (typeof user !== 'undefined' && user) { %>
                <div class="relative" x-data="{ userMenuOpen: false }">
                  <button 
                    @click="userMenuOpen = !userMenuOpen"
                    @click.outside="userMenuOpen = false"
                    class="flex items-center gap-2 pl-2 focus:outline-none group hover:opacity-80 transition-opacity"
                  >
                    <img src="/img/user-defaul.png" alt="Perfil" class="w-6 h-6 rounded-full object-cover border-2 border-gray-200 group-hover:border-app-green transition-colors">
                    <span class="text-sm font-medium text-app-deep hidden sm:inline-block group-hover:text-app-green transition-colors">
                      <%= user.nombre %>. <%= user.apellido ? user.apellido.charAt(0) : '' %>
                    </span>
                    <iconify-icon icon="solar:alt-arrow-down-bold" class="text-app-muted group-hover:text-app-green transition-colors hidden sm:inline-block" width="14"></iconify-icon>
                  </button>

                  <div 
                    x-show="userMenuOpen" 
                    x-transition:enter="transition ease-out duration-200"
                    x-transition:enter-start="opacity-0 translate-y-2"
                    x-transition:enter-end="opacity-100 translate-y-0"
                    x-transition:leave="transition ease-in duration-150"
                    x-transition:leave-start="opacity-100 translate-y-0"
                    x-transition:leave-end="opacity-0 translate-y-2"
                    x-cloak
                    class="absolute right-0 mt-3 w-56 bg-white rounded-2xl shadow-xl border border-gray-100 py-2 z-50"
                    style="display: none;"
                  >
                    <div class="px-4 py-3 border-b border-gray-100">
                      <p class="text-xs text-app-muted mb-1">Conectado como</p>
                      <p class="text-sm font-semibold text-app-deep truncate"><%= user.email %></p>
                    </div>
                    <a href="/perfil" class="flex items-center gap-3 px-4 py-3 text-sm text-app-deep hover:bg-gray-50 transition-colors">
                      <iconify-icon icon="solar:user-circle-bold" class="text-app-muted" width="18"></iconify-icon>
                      <span>Mi perfil</span>
                    </a>
                    <a href="/configuracion" class="flex items-center gap-3 px-4 py-3 text-sm text-app-deep hover:bg-gray-50 transition-colors">
                      <iconify-icon icon="solar:settings-bold" class="text-app-muted" width="18"></iconify-icon>
                      <span>Configuración</span>
                    </a>
                    <div class="border-t border-gray-100 my-1"></div>
                    <button 
                      @click="logout()" 
                      class="w-full flex items-center gap-3 px-4 py-3 text-sm text-red-500 hover:bg-red-50 transition-colors text-left"
                    >
                      <iconify-icon icon="solar:logout-2-bold" width="18"></iconify-icon>
                      <span>Cerrar sesión</span>
                    </button>
                  </div>
                </div>
              <% } else { %>
                <a href="/login" class="btn btn-ghost" aria-label="Iniciar sesión">
                  Iniciar sesión
                </a>
              <% } %>
            </div>
          </div>
          <div class="nav-divider"></div>
          <nav class="nav-links">
            <a href="/" class="nav-link">Inicio</a>
            <a href="/productos" class="nav-link">Productos</a>
            <a href="/colecciones" class="nav-link">Colecciones</a>
            <a href="/contacto" class="nav-link">Contacto</a>
          </nav>
        </div>
      </header>

      <main class="flex-1">
        <section class="hero-section">
          <div class="max-w-6xl mx-auto px-6 py-14 grid gap-10 lg:grid-cols-[1fr,420px]">
            <div class="space-y-6">
              <p class="inline-block rounded-full bg-white/60 px-4 py-1 text-sm font-medium text-app-green">
                Nuevo
              </p>
              <h1 class="text-4xl font-semibold leading-tight text-app-deep lg:text-5xl">
                Explora un catálogo diseñado para inspirarte y filtrar lo que realmente
                necesitas.
              </h1>
              <p class="text-lg text-app-muted">
                Usa filtros inteligentes, colecciones destacadas y una búsqueda potenciada por
                Fuse.js para encontrar productos con precisión.
              </p>
              <div class="flex flex-col gap-4 sm:flex-row">
                <div class="flex flex-1 items-center gap-3 rounded-2xl bg-white px-4 shadow-lg">
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    class="h-5 w-5 text-app-muted"
                  >
                    <circle cx="11" cy="11" r="7" />
                    <path d="m20 20-3.5-3.5" />
                  </svg>
                  <input
                    type="search"
                    placeholder="Buscar producto, etiqueta o colección"
                    class="h-12 w-full border-0 bg-transparent text-base text-app-dark focus:outline-none"
                    x-model="searchQuery"
                    @input="filterProducts"
                    aria-label="Buscar en el catálogo"
                  />
                </div>
                <button class="btn btn-secondary" @click="resetFilters">Limpiar filtros</button>
              </div>
            </div>
            <div class="hero-card">
              <p class="text-app-green text-sm font-semibold uppercase tracking-wide">
                Resumen dinámico
              </p>
              <h2 class="text-3xl font-semibold text-app-dark">+<span x-text="products.length"></span> productos
                curados</h2>
              <p class="text-app-muted">
                Filtra por categoría, precio y etiquetas para obtener recomendaciones en
                segundos.
              </p>
              <dl class="mt-6 space-y-4">
                <div class="stat-row">
                  <dt>Resultados activos</dt>
                  <dd x-text="filteredProducts.length + ' coincidencias'">0 coincidencias</dd>
                </div>
                <div class="stat-row">
                  <dt>Etiquetas seleccionadas</dt>
                  <dd x-text="selectedTags.length ? selectedTags.join(', ') : 'Ninguna'">Ninguna</dd>
                </div>
                <div class="stat-row">
                  <dt>Top valoración</dt>
                  <dd>
                    <span x-text="highlightedProduct.name"></span>
                    · <span x-text="highlightedProduct.rating"></span>/5
                  </dd>
                </div>
              </dl>
            </div>
          </div>
        </section>

        <section class="max-w-6xl mx-auto px-6 py-12 space-y-6">
          <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
            <div>
              <p class="text-sm uppercase tracking-wide text-app-muted">Colecciones</p>
              <h2 class="text-2xl font-semibold text-app-deep">Productos destacados</h2>
            </div>
            <div class="flex gap-3 text-sm text-app-muted">
              <span class="inline-flex items-center gap-1">
                <span class="dot dot-green"></span> Disponible
              </span>
              <span class="inline-flex items-center gap-1">
                <span class="dot dot-orange"></span> Tendencia
              </span>
            </div>
          </div>
          <div class="featured-swiper swiper">
            <div class="swiper-wrapper">
              <template x-for="product in featuredProducts" :key="product.id">
                <article class="swiper-slide featured-card">
                  <img :src="product.image" :alt="product.name" class="featured-img" />
                  <div class="space-y-3">
                    <p class="text-xs uppercase tracking-[0.2em] text-app-green" x-text="product.category"></p>
                    <h3 class="text-xl font-semibold text-app-dark" x-text="product.name"></h3>
                    <p class="text-sm text-app-muted" x-text="product.description"></p>
                    <div class="flex items-center justify-between">
                      <span class="text-2xl font-semibold text-app-deep" x-text="formatCurrency(product.price)"></span>
                      <span class="text-sm font-medium text-app-green" x-text="product.rating + '★'"></span>
                    </div>
                  </div>
                </article>
              </template>
            </div>
            <div class="swiper-pagination"></div>
          </div>
        </section>

        <section class="catalog-wrapper">
          <div class="max-w-6xl mx-auto grid gap-8 px-6 py-12 lg:grid-cols-[300px,1fr]">
            <aside class="filter-panel" aria-label="Filtros avanzados">
              <header class="flex items-center justify-between">
                <div>
                  <p class="text-xs uppercase tracking-wide text-app-muted">Personaliza</p>
                  <h2 class="text-xl font-semibold text-app-deep">Filtrado inteligente</h2>
                </div>
                <button class="text-sm font-semibold text-app-green" @click="resetFilters">
                  Reiniciar
                </button>
              </header>

              <div class="filter-section">
                <p class="filter-title">Categorías</p>
                <ul class="space-y-2" role="list">
                  <li>
                    <label class="filter-option">
                      <input
                        type="radio"
                        name="category"
                        value="all"
                        x-model="selectedCategory"
                        @change="filterProducts"
                      />
                      <span>Todos</span>
                    </label>
                  </li>
                  <template x-for="category in categories" :key="category">
                    <li>
                      <label class="filter-option">
                        <input
                          type="radio"
                          name="category"
                          :value="category"
                          x-model="selectedCategory"
                          @change="filterProducts"
                        />
                        <span x-text="category"></span>
                      </label>
                    </li>
                  </template>
                </ul>
              </div>

              <div class="filter-section">
                <p class="filter-title">Precio máximo</p>
                <div class="space-y-2">
                  <input
                    type="range"
                    min="50"
                    max="1500"
                    step="50"
                    x-model.number="priceCap"
                    @input="filterProducts"
                    aria-label="Límite de precio"
                  />
                  <p class="text-sm text-app-muted">
                    Hasta <span class="font-semibold" x-text="formatCurrency(priceCap)"></span>
                  </p>
                </div>
              </div>

              <div class="filter-section">
                <p class="filter-title">Etiquetas</p>
                <div class="flex flex-wrap gap-3">
                  <template x-for="tag in tags" :key="tag">
                    <label class="chip">
                      <input
                        type="checkbox"
                        class="sr-only"
                        :value="tag"
                        x-model="selectedTags"
                        @change="filterProducts"
                      />
                      <span x-text="tag"></span>
                    </label>
                  </template>
                </div>
              </div>

              <div class="filter-section">
                <p class="filter-title">Ordenar por</p>
                <select class="filter-select" x-model="sortOption" @change="filterProducts">
                  <option value="featured">Destacados</option>
                  <option value="price-asc">Precio (menor a mayor)</option>
                  <option value="price-desc">Precio (mayor a menor)</option>
                  <option value="rating">Mejor valoración</option>
                </select>
              </div>
            </aside>

            <section class="space-y-6" aria-live="polite">
              <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
                <div>
                  <p class="text-sm text-app-muted">Mostrando</p>
                  <h2 class="text-2xl font-semibold text-app-deep">
                    <span x-text="filteredProducts.length"></span> resultados relevantes
                  </h2>
                </div>
                <div class="flex flex-wrap gap-3" x-show="hasActiveFilters" x-cloak>
                  <template x-for="chip in activeFilterLabels" :key="chip">
                    <span class="active-chip" x-text="chip"></span>
                  </template>
                </div>
              </div>

              <div class="grid gap-6 md:grid-cols-2 xl:grid-cols-3" x-ref="productGrid">
                <template x-for="product in filteredProducts" :key="product.id">
                  <article class="product-card">
                    <img :src="product.image" :alt="product.name" class="product-img" />
                    <div class="product-body">
                      <div class="flex items-center justify-between">
                        <span class="product-category" x-text="product.category"></span>
                        <span class="product-rating" x-text="product.rating + '★'"></span>
                      </div>
                      <h3 class="product-title" x-text="product.name"></h3>
                      <p class="product-description" x-text="product.description"></p>
                      <div class="flex flex-wrap gap-2">
                        <template x-for="tag in product.tags" :key="tag">
                          <span class="tag" x-text="tag"></span>
                        </template>
                      </div>
                      <div class="product-footer">
                        <span class="product-price" x-text="formatCurrency(product.price)"></span>
                        <button class="btn btn-outline btn-xs">Ver detalles</button>
                      </div>
                    </div>
                  </article>
                </template>
                <div
                  class="empty-state"
                  x-show="!filteredProducts.length"
                  x-cloak
                >
                  <p class="text-lg font-semibold text-app-deep">Sin resultados</p>
                  <p class="text-sm text-app-muted">
                    Ajusta los filtros o prueba con otros términos de búsqueda.
                  </p>
                </div>
              </div>
            </section>
          </div>
        </section>
      </main>

      <footer class="bg-app-deep text-white">
        <div class="max-w-6xl mx-auto flex flex-col gap-4 px-6 py-8 text-sm text-white/80 md:flex-row md:items-center md:justify-between">
          <p>© <span x-text="new Date().getFullYear()"></span> Catálogo inteligente.</p>
          <div class="flex flex-wrap items-center gap-4">
            <a href="/terminos" class="nav-link">Términos</a>
            <a href="/privacidad" class="nav-link">Privacidad</a>
            <a href="/soporte" class="nav-link">Soporte</a>
            <a href="/dashboard" class="btn btn-dashboard" aria-label="Ir al panel de administración">
              Dashboard
            </a>
          </div>
        </div>
      </footer>
    </div>
      <script src="/assets/js/toast.js"></script>
  
  <script>
    // Mostrar toast basado en parámetros de URL
    window.addEventListener('DOMContentLoaded', () => {
      const urlParams = new URLSearchParams(window.location.search);
      
      if (urlParams.get('loggedIn') === 'true') {
        setTimeout(() => showToast('¡Bienvenido! Has iniciado sesión correctamente', 'success'), 100);
        window.history.replaceState({}, document.title, window.location.pathname);
      }
      
      if (urlParams.get('registered') === 'true') {
        setTimeout(() => showToast('¡Registro exitoso! Bienvenido a Catalogo360', 'success'), 100);
        window.history.replaceState({}, document.title, window.location.pathname);
      }
      
      if (urlParams.get('loggedOut') === 'true') {
        setTimeout(() => showToast('Has cerrado sesión correctamente', 'info'), 100);
        window.history.replaceState({}, document.title, window.location.pathname);
      }
    });
  </script>
    <script src="https://unpkg.com/swiper@9/swiper-bundle.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/fuse.js@6.6.2" defer></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js" crossorigin="anonymous" referrerpolicy="no-referrer" defer></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js" crossorigin="anonymous" referrerpolicy="no-referrer" defer></script>
    <script src="https://code.iconify.design/iconify-icon/2.1.0/iconify-icon.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/dinero.js@1.9.1/build/umd/dinero.min.js" defer></script>
    <script src="/assets/js/index-home.js" defer></script>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
  </body>
</html>
