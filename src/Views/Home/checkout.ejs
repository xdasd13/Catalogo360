<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Checkout | Catalogo360</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <link rel="icon" href="/img/favicon.ico" />
    <link rel="stylesheet" href="/assets/css/tailwind.css" />
    <link rel="stylesheet" href="/assets/css/index-home.css" />
    <style>
        .checkout-container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 2rem 1.5rem;
        }

        .form-section {
            background: #fff;
            border-radius: 24px;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .form-input {
            width: 100%;
            padding: 0.75rem 1rem;
            border-radius: 12px;
            border: 1px solid #e2e8f0;
            background-color: #f8fafc;
            transition: all 0.2s;
        }

        .form-input:focus {
            border-color: var(--app-green);
            background-color: #fff;
            outline: none;
            box-shadow: 0 0 0 3px rgba(52, 142, 145, 0.1);
        }

        .summary-card {
            background: #f8fafc;
            border-radius: 20px;
            padding: 1.5rem;
            border: 1px solid #e2e8f0;
        }
    </style>
</head>

<body class="bg-app-gray font-sans text-app-dark">
    <header class="nav-header">
        <div class="max-w-6xl mx-auto px-6 py-5 flex items-center justify-between">
            <a href="/" class="nav-logo" aria-label="Volver al inicio">
                <img src="/img/favicon.ico" alt="Catalogo360" class="nav-logo-icon" />
                <span class="nav-logo-text">Catalogo360</span>
            </a>
            <a href="/" class="text-sm font-semibold text-app-muted hover:text-app-deep">
                Seguir comprando
            </a>
        </div>
    </header>

    <main class="checkout-container">
        <h1 class="text-3xl font-bold text-app-deep mb-8">Finalizar Compra</h1>

        <div class="grid gap-8 lg:grid-cols-[1fr,380px]">
            <!-- Formulario -->
            <div class="space-y-6">
                <section class="form-section">
                    <h2 class="text-xl font-semibold mb-6 flex items-center gap-2">
                        <span
                            class="flex items-center justify-center w-8 h-8 rounded-full bg-app-green text-white text-sm">1</span>
                        Información de contacto
                    </h2>
                    <div class="grid gap-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Correo electrónico</label>
                            <input type="email" class="form-input" placeholder="tu@email.com" required>
                        </div>
                    </div>
                </section>

                <section class="form-section">
                    <h2 class="text-xl font-semibold mb-6 flex items-center gap-2">
                        <span
                            class="flex items-center justify-center w-8 h-8 rounded-full bg-app-green text-white text-sm">2</span>
                        Dirección de envío
                    </h2>
                    <div class="grid gap-4 md:grid-cols-2">
                        <div class="md:col-span-2">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Nombre completo</label>
                            <input type="text" class="form-input" required>
                        </div>
                        <div class="md:col-span-2">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Dirección</label>
                            <input type="text" class="form-input" placeholder="Calle, número, dpto..." required>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Ciudad</label>
                            <input type="text" class="form-input" required>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Código Postal</label>
                            <input type="text" class="form-input" required>
                        </div>
                        <div class="md:col-span-2">
                            <label class="block text-sm font-medium text-gray-700 mb-1">Teléfono</label>
                            <input type="tel" class="form-input" required>
                        </div>
                    </div>
                </section>

                <section class="form-section">
                    <h2 class="text-xl font-semibold mb-6 flex items-center gap-2">
                        <span
                            class="flex items-center justify-center w-8 h-8 rounded-full bg-app-green text-white text-sm">3</span>
                        Pago
                    </h2>
                    <div class="p-4 border border-app-green/30 bg-green-50 rounded-xl text-app-green-dark text-sm">
                        <p class="font-semibold">💳 Pasarela de pago de demostración</p>
                        <p>En esta versión demo, el pago se simulará al confirmar el pedido.</p>
                    </div>
                </section>
            </div>

            <!-- Resumen -->
            <div class="lg:sticky lg:top-8 h-fit">
                <div class="summary-card">
                    <h3 class="text-lg font-semibold text-app-deep mb-4">Resumen del pedido</h3>
                    <div id="checkout-items" class="space-y-4 mb-6 max-h-80 overflow-y-auto pr-2">
                        <!-- Items injected by JS -->
                        <p class="text-gray-500 text-sm text-center py-4">Cargando carrito...</p>
                    </div>

                    <div class="space-y-3 border-t border-gray-200 pt-4 text-sm">
                        <div class="flex justify-between text-gray-600">
                            <span>Subtotal</span>
                            <span id="checkout-subtotal">S/ 0.00</span>
                        </div>
                        <div class="flex justify-between text-gray-600">
                            <span>Envío</span>
                            <span>Gratis</span>
                        </div>
                        <div class="flex justify-between font-bold text-lg text-app-deep pt-2 border-t border-gray-200">
                            <span>Total</span>
                            <span id="checkout-total">S/ 0.00</span>
                        </div>
                    </div>

                    <button id="confirm-order-btn"
                        class="w-full mt-6 btn btn-primary py-3 rounded-xl shadow-lg hover:shadow-xl transition-all">
                        Confirmar pedido
                    </button>
                </div>
            </div>
        </div>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const itemsContainer = document.getElementById('checkout-items');
            const subtotalEl = document.getElementById('checkout-subtotal');
            const totalEl = document.getElementById('checkout-total');

            const getCart = () => {
                const cart = localStorage.getItem('cartItems');
                return cart ? JSON.parse(cart) : [];
            };

            const formatCurrency = (amount) => `S/ ${amount.toFixed(2)}`;

            const renderCheckout = () => {
                const cart = getCart();

                if (cart.length === 0) {
                    itemsContainer.innerHTML = '<p class="text-center text-gray-500">Tu carrito está vacío. <a href="/" class="text-app-green underline">Volver a la tienda</a></p>';
                    subtotalEl.textContent = formatCurrency(0);
                    totalEl.textContent = formatCurrency(0);
                    return;
                }

                let total = 0;
                itemsContainer.innerHTML = cart.map(item => {
                    total += item.price * item.quantity;
                    return `
              <div class="flex gap-3">
                <div class="w-16 h-16 rounded-lg bg-gray-100 overflow-hidden flex-shrink-0">
                  <img src="${item.image}" alt="${item.name}" class="w-full h-full object-cover">
                </div>
                <div class="flex-1 min-w-0">
                  <p class="text-sm font-medium text-gray-900 truncate">${item.name}</p>
                  <p class="text-xs text-gray-500">Cant: ${item.quantity}</p>
                </div>
                <p class="text-sm font-semibold text-gray-900">${formatCurrency(item.price * item.quantity)}</p>
              </div>
            `;
                }).join('');

                subtotalEl.textContent = formatCurrency(total);
                totalEl.textContent = formatCurrency(total);
            };

            renderCheckout();

            document.getElementById('confirm-order-btn').addEventListener('click', () => {
                const cart = getCart();
                if (cart.length === 0) return;

                // Simular proceso
                const btn = document.getElementById('confirm-order-btn');
                const originalText = btn.textContent;
                btn.textContent = 'Procesando...';
                btn.disabled = true;

                setTimeout(() => {
                    alert('¡Pedido confirmado! Gracias por tu compra.');
                    localStorage.removeItem('cartItems');
                    window.location.href = '/';
                }, 1500);
            });
        });
    </script>
    <script src="https://code.iconify.design/iconify-icon/2.1.0/iconify-icon.min.js"></script>
</body>
</html>