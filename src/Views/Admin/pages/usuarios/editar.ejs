<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuario - Panel de Administración</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/assets/css/dashboard.css">
    <link rel="stylesheet" href="/assets/css/crear.css">
</head>

<body class="bg-app-gray font-sans antialiased overflow-hidden">
    <div class="flex h-screen">
        <%- include('../../Layouts/sidebar') %>
            <div id="mainContent" class="flex-1 flex flex-col min-w-0">
                <%- include('../../Layouts/navbar') %>
                    <main class="flex-1 overflow-x-hidden overflow-y-auto p-6 md:p-8">

                        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

                            <!-- Columna Principal: Datos del Usuario -->
                            <div class="lg:col-span-2">
                                <div class="form-container">
                                    <div class="form-header">
                                        <h1 class="form-title">Editar Usuario</h1>
                                        <p class="form-subtitle">Actualiza la información del usuario</p>
                                    </div>

                                    <% if (error && error.length> 0) { %>
                                        <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
                                            <p class="text-red-800 text-sm">
                                                <%= error %>
                                            </p>
                                        </div>
                                        <% } %>

                                            <% if (message && message.length> 0) { %>
                                                <div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-lg">
                                                    <p class="text-green-800 text-sm">
                                                        <%= message %>
                                                    </p>
                                                </div>
                                                <% } %>

                                                    <form action="/admin/usuarios/<%= usuario.id %>?_method=PUT"
                                                        method="POST">

                                                        <!-- Información Personal -->
                                                        <div class="border-b border-gray-100 pb-6 mb-6">
                                                            <h3 class="text-lg font-semibold text-app-deep mb-4">
                                                                Información Personal</h3>
                                                            <div class="form-grid form-grid-cols-2">
                                                                <div class="form-group">
                                                                    <label class="form-label form-label-required"
                                                                        for="nombre">Nombre</label>
                                                                    <input type="text" id="nombre" name="nombre"
                                                                        class="form-input" required
                                                                        value="<%= usuario.nombre %>">
                                                                </div>

                                                                <div class="form-group">
                                                                    <label class="form-label"
                                                                        for="apellido">Apellido</label>
                                                                    <input type="text" id="apellido" name="apellido"
                                                                        class="form-input"
                                                                        value="<%= usuario.apellido || '' %>">
                                                                </div>

                                                                <div class="form-group">
                                                                    <label class="form-label"
                                                                        for="telefono">Teléfono</label>
                                                                    <input type="tel" id="telefono" name="telefono"
                                                                        class="form-input"
                                                                        value="<%= usuario.telefono || '' %>">
                                                                </div>

                                                                <div class="form-group">
                                                                    <label class="form-label form-label-required"
                                                                        for="email">Correo Electrónico</label>
                                                                    <input type="email" id="email" name="email"
                                                                        class="form-input" required
                                                                        value="<%= usuario.email %>">
                                                                </div>
                                                            </div>
                                                        </div>

                                                        <!-- Roles y Permisos -->
                                                        <div class="border-b border-gray-100 pb-6 mb-6">
                                                            <h3 class="text-lg font-semibold text-app-deep mb-4">Roles y
                                                                Permisos</h3>

                                                            <div class="space-y-3">
                                                                <% if (roles && roles.length> 0) { %>
                                                                    <% roles.forEach(rol=> {
                                                                        const isChecked = userRoleIds.includes(rol.id);
                                                                        %>
                                                                        <label
                                                                            class="flex items-start gap-3 p-3 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer transition-colors <%= isChecked ? 'bg-blue-50 border-blue-200' : '' %>">
                                                                            <input type="checkbox" name="roles"
                                                                                value="<%= rol.id %>" <%=isChecked
                                                                                ? 'checked' : '' %>
                                                                            class="mt-1 form-checkbox text-app-green
                                                                            rounded focus:ring-app-green">
                                                                            <div>
                                                                                <span
                                                                                    class="block font-medium text-gray-900">
                                                                                    <%= rol.nombre %>
                                                                                </span>
                                                                                <span
                                                                                    class="block text-sm text-gray-500">
                                                                                    <%= rol.descripcion %>
                                                                                </span>
                                                                            </div>
                                                                        </label>
                                                                        <% }); %>
                                                                            <% } else { %>
                                                                                <p class="text-sm text-gray-500 italic">
                                                                                    No hay roles definidos en el
                                                                                    sistema.</p>
                                                                                <% } %>
                                                            </div>
                                                        </div>

                                                        <!-- Estado -->
                                                        <div class="mb-6">
                                                            <label class="checkbox-label">
                                                                <input type="checkbox" name="activo" value="1"
                                                                    class="form-checkbox" <%=usuario.activo ? 'checked'
                                                                    : '' %>>
                                                                <span>Usuario Activo</span>
                                                            </label>
                                                            <p class="text-xs text-gray-500 mt-1 ml-7">Los usuarios
                                                                inactivos no pueden iniciar sesión.</p>
                                                        </div>

                                                        <!-- Acciones -->
                                                        <div class="form-actions">
                                                            <a href="/admin/usuarios" class="btn btn-secondary">
                                                                <i class='bx bx-x'></i> Cancelar
                                                            </a>
                                                            <button type="submit" class="btn btn-primary">
                                                                <i class='bx bx-check'></i> Actualizar Usuario
                                                            </button>
                                                        </div>
                                                    </form>
                                </div>
                            </div>

                            <!-- Columna Lateral: Cambiar Contraseña -->
                            <div class="lg:col-span-1">
                                <div class="bg-white rounded-xl shadow-card border border-gray-100 p-6">
                                    <h3 class="text-lg font-semibold text-app-deep mb-4">Cambiar Contraseña</h3>
                                    <p class="text-sm text-gray-500 mb-4">Si no deseas cambiar la contraseña, deja estos
                                        campos vacíos.</p>

                                    <form action="/admin/usuarios/<%= usuario.id %>/password?_method=PUT" method="POST">
                                        <div class="form-group mb-4">
                                            <label class="form-label" for="password">Nueva Contraseña</label>
                                            <input type="password" id="password" name="password" class="form-input"
                                                placeholder="Mínimo 6 caracteres">
                                        </div>

                                        <div class="form-group mb-6">
                                            <label class="form-label" for="confirm_password">Confirmar
                                                Contraseña</label>
                                            <input type="password" id="confirm_password" name="confirm_password"
                                                class="form-input" placeholder="Repite la contraseña">
                                        </div>

                                        <button type="submit"
                                            class="w-full py-2 bg-gray-800 text-white rounded-lg hover:bg-gray-900 transition-colors text-sm font-medium">
                                            Actualizar Contraseña
                                        </button>
                                    </form>
                                </div>

                                <div class="mt-6 bg-white rounded-xl shadow-card border border-gray-100 p-6">
                                    <h3 class="text-lg font-semibold text-app-deep mb-4">Información de Registro</h3>
                                    <ul class="space-y-3 text-sm">
                                        <li class="flex justify-between">
                                            <span class="text-gray-500">Registrado:</span>
                                            <span class="font-medium text-gray-900">
                                                <%= new Date(usuario.creado_en).toLocaleDateString() %>
                                            </span>
                                        </li>
                                        <li class="flex justify-between">
                                            <span class="text-gray-500">Última actualización:</span>
                                            <span class="font-medium text-gray-900">
                                                <%= usuario.actualizado_en ? new
                                                    Date(usuario.actualizado_en).toLocaleDateString() : '-' %>
                                            </span>
                                        </li>
                                        <li class="flex justify-between">
                                            <span class="text-gray-500">Último acceso:</span>
                                            <span class="font-medium text-gray-900">
                                                <%= usuario.ultimo_acceso ? new
                                                    Date(usuario.ultimo_acceso).toLocaleDateString() : 'Nunca' %>
                                            </span>
                                        </li>
                                    </ul>
                                </div>
                            </div>

                        </div>

                    </main>
            </div>
    </div>
    <script src="/assets/js/dashboard.js"></script>
</body>

</html>